/** Shared Design prompt length (client + server + UI). */
export const MAX_DESIGN_PROMPT_LENGTH = 8000;

/** Soft warning threshold for the character counter. */
export const DESIGN_PROMPT_WARN_LENGTH = 7200;

export const DESIGN_PROMPT_LIMIT_MESSAGE = "Maximum 8,000 characters";
